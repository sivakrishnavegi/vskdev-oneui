// Import only the compiled CSS (not raw Tailwind source)
import "../dist/oneui.css";

// Export UI components
export * from "./components/Button";

// Optional sample export
export const hello = "One UI Ready!";
